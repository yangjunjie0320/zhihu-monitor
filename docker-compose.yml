services:
  monitor:
    build: .
    container_name: zhihu-monitor
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - ./data:/app/data
      - ../logs:/app/data/logs
      - ./cookies:/app/cookies:ro
